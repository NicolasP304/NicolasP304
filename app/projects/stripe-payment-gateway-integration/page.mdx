import Link from "next/link";

export const metadata = {
  title: "Stripe Payment Gateway Integration | Nicolas Papageorgiou",
  description:
    "Case study on implementing secure Stripe payments in enterprise intranet workflows and delivering +200% revenue impact."
};

<main className="mx-auto w-[min(900px,92vw)] py-12 sm:py-16">
  <p className="text-sm font-semibold uppercase tracking-[0.08em] text-[color:var(--text-muted)]">Case study</p>
  <h1>Stripe Payment Gateway Integration</h1>
  <p>
    This delivery focused on introducing secure, reliable online payments into an enterprise intranet workflow that
    previously relied on slower manual processing.
  </p>

  <h2>Context</h2>
  <p>
    The client required payment support directly inside intranet form submissions while preserving auditability,
    validation, and existing permissions rules. The implementation had to fit production constraints and avoid
    disruption to active operations.
  </p>

  <h2>Approach</h2>
  <ul>
    <li>Implemented Stripe payment flows in the existing PHP-based form pipeline.</li>
    <li>Added server-side validation and secure token handling.</li>
    <li>Integrated status feedback so operations teams could reconcile submissions confidently.</li>
    <li>Tested error paths for declined cards, partial failures, and retry scenarios.</li>
  </ul>

  <h2>Outcome</h2>
  <ul>
    <li>Improved transaction reliability across intranet workflows.</li>
    <li>Reduced manual payment handling overhead.</li>
    <li>
      Increased client revenue from <strong>$5,000</strong> to <strong>$15,000</strong> (around <strong>+200%</strong>).
    </li>
  </ul>

  <Link href="/projects" className="mt-8 inline-flex text-sm font-semibold text-[color:var(--brand)] hover:underline">
    Back to all projects
  </Link>
</main>
